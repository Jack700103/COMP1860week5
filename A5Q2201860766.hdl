CHIP A5Q2201860766 {
    IN N, L, K;
    OUT H;   

    PARTS:
        Not(in=L, out=notL);
        Or(a=N, b=notL, out=orOut);
        And(a=N, b=notL, out=andOut);
        Or(a=orOut, b=andOut, out=orFinal);
        And(a=K, b=orFinal, out=H);
}
