CHIP A5Q2<201860766> {
    IN N, L, K;    
    OUT H;         

    PARTS:
        Not(in=N, out=not_N);
        And(a=not_N, b=K, out=and1_out);
        Or(a=and1_out, b=L, out=or_out);
        And(a=or_out, b=K, out=H);
}